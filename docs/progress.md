# OneResume 进展文档

## 2026-03-28

### 阶段 0：项目骨架

- 已初始化 Vue 3 + TypeScript + Vite 项目。
- 已接入 vue-router、pinia、tailwindcss、markdown-it、unified、remark-parse、idb、vuedraggable@next、lucide-vue-next 等核心依赖。
- 已补齐基础测试、类型检查和构建命令，保证后续迭代可验证。

### 阶段 1：本地数据层

- 已完成简历领域模型、示例数据和 schema 迁移入口。
- 已完成 localStorage 的工作区索引与简历文档持久化。
- 已完成 IndexedDB 的头像资源存储与引用读取。
- 已完成 JSON / Markdown 的基础导入导出能力。

### 阶段 2：MVP 工作台

- 已完成顶部工具栏、多简历切换、新建、重命名、删除、重置与清空本地数据。
- 已完成左侧结构化编辑器，支持基础信息、栏目显隐、重命名、条目增删改和拖拽排序。
- 已完成右侧 A4 实时预览，支持多模板、样式调整和缩放。
- 已完成打印路由与浏览器打印导出 PDF 闭环。
- 已完成头像上传、裁剪、本地持久化与移除。

### 阶段 3：验证与构建

- 
- 已通过 npm run test。
- 已通过 npm run typecheck。
- 已通过 npm run build。

### 阶段 4：Markdown / HTML 导入兼容

- 已识别并兼容“Markdown 后缀但实际包含 HTML 简历”的导入情况。
- 已扩展导入器，支持在 Markdown 中自动识别 HTML 内容并分流解析。
- 已补充中文栏目标题映射，兼容“教育背景 / 实习经历 / 项目经历 / 专业技能”等常见命名。
- 已补充对应测试，覆盖基础信息、教育、经历、项目与技能解析。

### 阶段 5：导入导出与界面收敛

- 已将工具栏整理为统一的“导入 / 导出”入口。
- 已支持按文件类型自动导入 HTML / HTM / Markdown / TXT / JSON。
- 已支持导出 PDF / Markdown / 工程文件，当来源为 HTML 时额外支持导出原始 HTML。
- 已新增 HTML 简历的“原模板预览 / 结构化预览”切换模式。
- 已将“专业技能”模块改为轻量文本输入，不再显示时间、地点、标题等不合适字段。

### 阶段 6：HTML 原模板预览修复

- 已将 HTML 原模板预览从 v-html 改为 iframe srcdoc 隔离渲染，避免站内样式污染导入模板。
- 已同步调整打印链路，HTML 原模板模式下对 iframe 内文档进行打印。
- 已增加本地图片路径提示，明确浏览器对磁盘图片访问的限制。

### 阶段 7：HTML 原模板少显示修复

- 已让 iframe 根据模板真实内容尺寸自适应，不再固定死 A4 容器。
- 已加入 ResizeObserver + 图片 load/error 监听 + requestAnimationFrame 的尺寸同步机制。
- 已修复缩放后底部裁切、占位高度不足和少显示问题。
- 已完成本地 test / typecheck / build 验证。

### 阶段 8：工作台高度对齐与面板化

- 已将工作台布局改为 flex column + main flex-1，避免依赖脆弱的 calc(100vh-xxx)。
- 已为左侧编辑区和右侧预览区引入统一的 workbench-panel 面板外壳，统一圆角、边框、阴影和内部滚动区域。
- 已将 HTML 本地图片提示收回到右侧预览面板内部，避免再次破坏左右高度对齐。
- 已通过 npm run typecheck。
- 已通过 npm run build。

### 阶段 9：头像配置与自定义模块闭环

- 已新增 photoPlacement，支持头像在简历中的左右位置切换，并兼容旧数据迁移。
- 已增强头像上传链路，仅接受图片文件，支持更换与移除。
- 已调整基础信息区的头像编辑卡片布局，让头像操作与个人信息表单分区更清晰。
- 已补齐自定义模块闭环：新增时自动生成首个内容块，支持编辑标题、说明、Markdown 内容，并支持删除。
- 已修复自定义模块在预览中重复显示副标题元信息的问题。
- 已通过 npm run test。
- 已通过 npm run typecheck。
- 已通过 npm run build。

### 阶段 10：同步预览入口

- 已为 HTML 原模板模式补充“同步预览”入口，顶部工具栏和左侧提示卡都可一键切换。
- 已补充明确提示文案，说明右侧会跟随左侧修改实时更新。
- 已通过 npm run test。
- 已通过 npm run typecheck。
- 已通过 npm run build。

### 阶段 11：上传 HTML 模板同步渲染

- 已新增 HTML 模板同步器，在保留上传模板原始样式骨架的前提下，把结构化编辑后的姓名、联系方式、头像和模块内容回填到模板中。
- 已新增 source-html-sync 预览模式，“同步预览”不再切回站内默认模板，而是继续使用上传的 HTML 模板实时更新。
- 已打通打印链路，上传模板同步预览状态下导出 PDF 也沿用同步后的 HTML 模板内容。
- 已新增对应测试，覆盖上传 HTML 模板的姓名、联系方式、项目、技能和头像同步。
- 已通过 npm run test。
- 已通过 npm run typecheck。
- 已通过 npm run build。

### 阶段 12：模块设置表单化

- 已为每个模块补充独立设置表单，支持直接修改模块名称。
- 已新增模块类型下拉，可在教育背景、工作经历、项目经历、专业技能和自定义模块之间切换，切换后条目表单随模块类型变化。
- 已新增模块顺序下拉，可直接移动到指定位置，同时保留拖拽排序能力。
- 已将“新增自定义模块”入口收敛成更通用的“新增模块”。
- 已通过 npm run test。
- 已通过 npm run typecheck。
- 已通过 npm run build。

### 阶段 13：头像比例调整

- 已将头像上传裁剪从默认 1:1 改为常见二寸证件照比例 35:49。
- 已统一左侧编辑卡片与右侧结构化简历模板中的头像展示比例，避免上传后和预览时比例不一致。
- 已补充头像裁剪比例单元测试，覆盖宽图和长图的居中裁剪场景。

### 阶段 14：顶部工具栏收敛与标签页图标

- 已移除顶部品牌宣传文案、缩放按钮组和工具栏里的头像快捷入口，减少视觉噪音。
- 已整理顶部工具栏与工作台页面中的中文文案，清理这一轮涉及区域的乱码文本。
- 已新增极简 SVG favicon，并接入页面标题与站点描述。

### 阶段 15：品牌区优化与文本加粗

- 已优化左上角品牌区，升级为图标加字标的一体化展示，整体更像产品工作台而不是占位标签。
- 已为个人简介、项目描述、工作经历描述、专业技能、自定义模块内容补充“选中文字一键加粗”能力，底层采用 Markdown **加粗** 包裹。
- 已补充文本包裹工具测试，并让预览区支持渲染个人简介中的 Markdown 加粗效果。

### 阶段 16：导出链路修复

- 已重构 Markdown 导出逻辑，结构化简历按当前模块顺序、显示状态和编辑内容导出。
- 已让 HTML 简历在同步预览模式下先按当前模板同步结果渲染，再转换为 Markdown，避免导出内容落回旧格式。
- 已让 Markdown 导出携带头像图片数据，导出后的文件不再丢头像。
- 已补强打印视图，导出 PDF 时会把页面标题设置为当前简历名，并在打印前等待图片加载，减少头像丢失。

### 阶段 17：Markdown 头像兼容修正

- 已将 Markdown 导出中的头像从超长 data: 图片链接语法改为内联 HTML <img> 标签，提高 Typora 等编辑器的兼容性。
- 已同步覆盖结构化导出与 HTML 同步模板导出两条链路，避免头像再次被拆成普通文本。

### 阶段 18：导出入口收敛

- 已按当前上线需求移除产品里的 Markdown 导出入口，避免用户继续走到未最终定稿的导出格式。
- 已同步收敛工具栏导出事件分支，当前保留 PDF / 工程文件 / 原始 HTML 三类导出能力。

### 阶段 19：Git 上传准备

- 已补充适合 Vue 3 + Vite + TypeScript 项目的 .gitignore，新增环境变量、构建缓存、覆盖率、测试产物和常见系统文件忽略规则。
- 已确认当前目录尚未初始化 Git 仓库，后续上传步骤需从 git init 开始。

### 阶段 20：导出文件名统一取值

- 已将 JSON / Markdown / HTML / PDF 导出时的默认文件名统一改为简历标题，去掉了后缀名冗余，提升了导出文件的规范性。
- 已重新通过 npm run typecheck。

### 阶段 21：新增商务蓝模板

- 新增 classic-blue 商务蓝模板，样式参考用户提供的优秀 HTML 简历，优化了字体间距、证件照位置和蓝色章节分割线。
- 已接入模板切换引擎，支持不同模板间的数据平滑迁移。
- 已通过 npm run test 与 npm run build。

### 阶段 22：智能 PDF 结构化解析

- 引入 smartPdfParser，尝试对上传的 PDF 文件进行文字坐标、字号与段落分析，提取结构化 HTML 内容。
- 当解析失败时，会自动回退到原有的纯文本提取模式，确保导入过程不中断。
- 已补充对应的单元测试，覆盖了基础信息、项目与技能的解析链路。
- 已通过 npm run build。

### 阶段 23：预览背景与样式优化

- 右侧结构化预览区增加了深灰色背景衬托，并为 A4 简历纸张添加了轻微阴影，提升视觉真实感。
- 修复了初始加载时预览区由于异步数据导致的纸张跳动问题。
- 已通过 npm run typecheck。

### 阶段 24：预览区交互微调

- 为预览区的纸张卡片增加了边框感与圆角（非打印态），使编辑与预览的界限更清晰。
- 调整了默认缩放比例，使其在 13/14 寸笔记本屏幕上能显示更完整的内容。
- 已通过 npm run build。

### 阶段 25：工作台布局升级调研

- 确认迁移方向：聚焦“简历编辑 + 模板系统”，重塑工作台布局。
- 确立三段式结构：左导航、中编辑、右预览。

### 阶段 26：工作台结构升级（JadeAI 风格）

- 正式重构工作台：左侧模块导航、中间聚焦编辑区、右侧实时预览区。
- 新增模块导航侧栏，支持在工作台中快速切换、拖拽排序、显示隐藏和新增模块。
- 已通过 npm run test 与 npm run build。

### 阶段 27：中间编辑区卡片化

- 为中间编辑区加入顶部卡片带，使基础信息与各模块以可点击卡片形式呈现。
- 强化了“聚焦卡片”的双层结构，增强了编辑的层次感。

### 阶段 28：编辑区 UI 收敛

- 移除了中间过重的卡片画布，回归更清爽的表单编辑器结构。
- 重做条目编辑框：统一了拖拽手柄、删除入口和空状态提示。

### 阶段 29：合并模块切换与设置

- 将中间编辑区顶部的“模块切换”与“模块设置”区域合并。
- 名称、类型、显示状态合并入同一个编辑面板头部。

### 阶段 30：移除编辑框内的模块顺序字段

- 移除了编辑面板内的“顺序”表单，改为由左侧导航列表统一管理，减少操作冲突。

### 阶段 31：左侧导航收缩能力

- 为左侧模块导航加入收起/展开功能，收缩后以紧凑图标栏展示，释放编辑空间。
- 已通过 npm run test。

### 阶段 32：模块名称头部直编

- 中间编辑区头部的模块名称改为可直接点击编辑的 Input，实现所见即所得。

### 阶段 33：清理冗余文案

- 移除了基础信息编辑区中“先把简历主干补完整”等冗余引导文案，界面更加干净。

### 阶段 34：增强富文本与专业技能合并

- 为“个人简介”、“项目描述”等字段增加了放大编辑弹窗。
- 引入轻量级 Markdown 工具栏，支持加粗、列表、标题的快速插入。
- 将“专业技能”从列表项改为统一的高级富文本块。

### 阶段 35：布局比例微调

- 调整了工作台三列比例：缩减了左侧导航，为中间编辑区和右侧预览区腾出更多空间。
- 预览区现在支持点击页码快速定位到顶部。

### 阶段 36：全面升级 Tiptap 富文本编辑器

- 放弃简单的 Textarea，引入 **Tiptap (ProseMirror)** 作为底层编辑器。
- 支持真正的加粗、斜体、标题及列表操作，底层以 HTML 格式存储，确保排版一致性。

### 阶段 37：富文本选区状态记忆

- 修复了在加粗/斜体状态下，光标在空行时工具栏按钮状态不准确的问题。
- 移除了浮动的工具栏，将其固定在编辑区顶部以保持布局稳定。

### 阶段 38：预览渲染稳定性增强

- 解决了富文本编辑时，右侧预览区由于频繁渲染导致的 DOM 闪烁问题。
- 已通过 npm run build。

### 阶段 39：富文本选区恢复逻辑优化

- 解决了点击工具栏加粗按钮时，编辑器失去焦点导致加粗失败的顽固 Bug。
- 将操作逻辑改为 mousedown 触发，确保选区在命令执行前不消失。

### 阶段 40：底层存储架构调整

- 简历数据模型正式由 descriptionMarkdown 迁移为 descriptionHtml。
- 编写了数据迁移脚本，确保老用户的 Markdown 内容在载入时自动转化为 HTML 格式。

### 阶段 41：导出文件名与头像修正

- 修复了 Markdown 导出中头像无法内联的问题，现在统一采用 Base64 嵌入。
- 更新了 .gitignore，过滤了所有构建产物和本地测试文件。

## 2026-04-03

### 阶段 42：样式面板重构

- 压缩了顶部工具栏高度，将全局样式（字号、行高、边距、主题色）下沉到左侧独立面板。
- 预览区引入 ResizeObserver，动态计算纸张缩放比例，完美解决底部溢出裁剪问题。

### 阶段 43：模板体系扩展与工作台收口

- 站内模板由 4 款扩展到 **10 款**（涵盖专业、ATS、高管等风格）。
- 新增左侧独立模板选择区，使用预览卡片切换模板。
- 统一了首页与工作台的品牌 Logo 视觉，完成整体 Landing Page 路由拆分。
- 已通过 npm run test、npm run typecheck 与 npm run build。

### 阶段 44：模板区改为独立模板页

- 新增 `/templates` 独立模板选择页，不再把模板区作为工作台布局下方的内联模块。
- 工作台顶部模板入口已改为“模板库”按钮，进入模板页时会携带当前简历与当前模板信息。
- 已完成“模板页选择模板 -> 回到 `/workspace` -> 立即应用到当前简历”的跳转链路，并在应用后自动清理临时查询参数。

### 阶段 45：10 款模板升级为多布局体系

- 重写了 `ResumeDocument`，将 10 款模板拆成多种布局族，而不是继续只做颜色和字体替换。
- 新增卡片式、紧凑双栏、优雅居中、高管头部强化、ATS 纯净版等结构，保留原有侧栏和蓝线两类传统版式。
- 模板预览页已经接入真实简历缩略预览，可以直接对比不同模板的布局差异。

### 阶段 46：模板页与多布局验收

- 已补充模板页烟雾测试，以及 `ResumeDocument` 对紧凑、高管、ATS 等多布局模板的结构断言。
- 已重新通过 `npm run typecheck`。
- 已重新通过 `npm run test`。
- 已重新通过 `npm run build`。

### 阶段 47：模板入口与模板页视觉收束

- 已将工作台中的“模板库”按钮从次级工具区上移到“新建”旁边，并增强了按钮识别度。
- 已移除模板选择页中多余的说明文案与模板描述，让页面更接近纯模板画廊视图。
- 已将模板页头部收敛为“品牌 + 简短标题 + 返回工作台”，并把模板网格扩展为更直接的浏览布局。

### 阶段 48：工作台顶栏继续收缩

- 已将“清空本地数据”并入顶部主功能栏，避免继续占用第二排空间。
- 已把“当前模板”标识移到简历预览面板标题右侧，让模板状态更贴近预览结果本身。
- 已进一步收紧工具栏纵向间距，使工作台顶部整体更紧凑。

### 阶段 49：底部提示条动效收束

- 已将工作台底部中间提示改为深色到青绿色的渐变样式，弱化原先纯深色提示的生硬感。
- 已为提示条加入淡入淡出过渡，并改为约 5 秒后自动消失，不再长时间停留在页面底部。
- 提示条现已设置为不拦截指针事件，避免遮挡底部预览区域操作。

### 阶段 50：提示条颜色与离场节奏微调

- 已将底部提示条改为更贴近工作台背景的灰白半透明样式，降低突兀感。
- 已拉长提示条淡出时长，并加入轻微模糊，让离场更像慢慢消失而不是直接消失。
- 已同步微调自动关闭计时，让"停留 + 淡出"整体仍维持在约 5 秒左右。

## 2026-04-04

### 阶段 51：富文本编辑器 UI 修正

- 已将工具栏和编辑区合并为一个带圆角边框的视觉整体，工具栏作为编辑区顶部条带底部分割线和半透明背景。
- 编辑区移除了自身独立的边框和圆角，改为纯透明内容区。
- 整个编辑器容器支持 `focus-within` 边框高亮和发光阴影，暗示可交互。
- 新增点击容器空白区域自动聚焦到编辑器的能力，解决了"点击文本框旁边无法选中"的问题。
- 工具栏按钮缩小为更紧凑的尺寸，加入圆角 hover 效果。

### 阶段 52：导入功能重构

- 新建 `importParsers.ts` 解析引擎，引入分层架构（原始内容 → ParsedResume 中间结构 → ResumeDocument 领域模型）。
- HTML 导入：支持每个章节多条目解析，智能检测条目标题行（h3/h4/带日期行/多 span div/粗体行），基础信息从全文搜索提取。
- Markdown 导入：自动检测章节 heading 层级（h1 或 h2），子 heading 自动作为条目分隔符。
- PDF 纯文本导入：基于行语义分组，不再依赖中间 Markdown 转换。
- 统一了时间范围提取，支持多种中英文日期格式。
- 扩展 `normalizeSectionHeading` 映射表，从约 20 个覆盖到约 70 个中英文简历章节标题。
- 清理了 `markdownExchange.ts` 和 `pdfExchange.ts` 中约 200 行旧导入代码。
- 已通过 npm run test（19 files, 70 tests, 全部通过）。
- 已通过 npm run typecheck。
- 已通过 npm run build。

### 阶段 53：修复 HTML 模板导入渲染与新增赞助名单

- 从 Markdown 流程导入被识别为 HTML 时强制不保留原 HTML 样式，避免非本站 HTML 模板导致缩放重叠割裂。
- 修复 importParsers.ts 中提取手机号格式化的测试断言问题。
- 在首页下方新增 LandingSponsorSection 赞助名单展示区，并调整 Tailwind 的 utility 使用方式。

### 阶段 54：首页赞助区轻调整与 process 忽略补充

- 将赞助名单区域收敛为与 Landing 其它板块一致的卡片式区块，统一标题层级、留白和边框语言。
- 调整首页区块顺序，把赞助名单放到最终 CTA 之前，让“开始使用”继续作为首页收尾动作。
- 为赞助名单补充概览信息与更稳定的卡片排版，适配后续继续新增赞助者。
- 在 `.gitignore` 中新增根目录 `process` 相关忽略规则，避免本地过程文件下次再进入 Git 提交。
### 阶段 55：修复进展文档编码

- 以 Git 中的干净版本为基底恢复 `docs/progress.md`，清理工作区中被误写入的乱码内容。
- 将进展文档统一以 UTF-8 编码重新保存，避免后续再次出现中文显示异常。
